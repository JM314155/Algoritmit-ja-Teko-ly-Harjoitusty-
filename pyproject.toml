[tool.poetry]
name = "kps.tekoaly"
version = "0.2.0"
description = "Kivi-Sakset-Paperi tekoaly harjoitustyo"
authors = ["nimi?"]


[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0"
coverage = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
